# Backend

FastAPI backend for Fake News Detection.

## Main files

- `core/main.py`: API routes and inference logic.
- `core/train.py`: model training pipeline.
- `models/`: trained model artifacts.
- `feedback/`: runtime feedback store (ignored from git).
