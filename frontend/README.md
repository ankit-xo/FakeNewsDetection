# Frontend

React + Vite client app for Fake News Detection.

## Scripts

- `npm run dev`: start local frontend server.
- `npm run build`: build production assets.
- `npm run deploy`: publish frontend to GitHub Pages.

## Notes

- Base path for production is `/FakeNewsDetection/`.
- Backend base URL is controlled by `VITE_API_BASE_URL`.
